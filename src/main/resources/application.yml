server:
  port: 8082
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/vod_test
    username: postgres
    password: postgres

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
